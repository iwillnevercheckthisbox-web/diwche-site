---
import Base from '../../layouts/Base.astro';
import Home from '../../components/Home.astro';
import { HOME_DE } from '../../content/home';
---

<Base
  locale="de"
  copy={HOME_DE}
  title={HOME_DE.meta.title}
  description={HOME_DE.meta.description}
>
  <Home copy={HOME_DE} locale="de" />
</Base>
