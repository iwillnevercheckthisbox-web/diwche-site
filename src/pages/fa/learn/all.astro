---
import LearnAll from '../../../components/learn/LearnAll.astro';
import { learnIndex } from '../../../lib/learn-paths';

const { all, nav } = await learnIndex('fa');
---

<LearnAll locale="fa" nav={nav} entries={all} />
